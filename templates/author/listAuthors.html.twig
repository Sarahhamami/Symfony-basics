<h1>list authors</h1>
 <table border="1">
        <tr>
            <th>#</th>
            <th>Username</th>
            <th>Email</th>
           <th>Actions</th>
        </tr>
      
{% for i in list %}
            <tr>
                <td>{{ i.id }}</td>
                <td>{{ i.username }}</td> 
                <td>{{ i.email }}</td>
                <td><a href="{{ path('editAuthor', {'id': i.id}) }}">edit</a>
                <a href="{{ path('deleteAuthor', {'id': i.id}) }}">delete</a></td>
            </tr>
        {% endfor %}
</table>